public with sharing class gender {
   /*  @AuraEnabled(cacheable=true)
    public static List<Doctor__c> returnDoctorsgender(String gen ){
        List<Doctor__c> doc= [SELECT Name,	Experince__c,	Address__c,Specialization__c,Email__c ,Gender__c FROM Doctor__c  WHERE Gender__c=:gen];
        return doc;

    } */
}